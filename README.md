# Store – Extension & Plugin Registry

This directory serves as the **central registry** for installable extensions, plugins, modules, templates and recipes used by FreeSynergy products.

## Current Namespaces

### node/
Everything specific to **FreeSynergy/Node** – the self-hosted, decentralized node & deployment engine.
