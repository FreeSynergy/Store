zentinel-bundle-description =
    The Zentinel bundle installs both the Zentinel reverse proxy and its
    control plane together — the recommended way to set up routing for a
    new FreeSynergy node.

zentinel-bundle-components =
    This bundle contains:

    - { $link-zentinel } — the reverse proxy server
    - { $link-zentinel-control-plane } — dynamic routing API

zentinel-bundle-note =
    Click a component to see its full description and configuration options.
