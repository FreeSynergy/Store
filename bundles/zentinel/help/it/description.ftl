zentinel-bundle-description =
    Il bundle Zentinel installa sia il reverse proxy Zentinel che il suo
    pannello di controllo insieme — il metodo consigliato per configurare
    il routing su un nuovo nodo FreeSynergy.

zentinel-bundle-components =
    Questo bundle include:

    - { $link-zentinel } — il server reverse proxy
    - { $link-zentinel-control-plane } — l'API di routing dinamico

zentinel-bundle-note =
    Clicca su un componente per visualizzare la descrizione completa e le opzioni di configurazione.
